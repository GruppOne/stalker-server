package tech.gruppone.stalker.server.services;

public class OrganizationServiceTest {

  // long userId = 1L;
  // String email = "gruppone.swe@gmail.it";
  // String firstName = "Gruppone";
  // String lastName = "Swe";
  // LocalDate birthdate = LocalDate.parse("1960-04-16");

  // var userDao = UserDao.builder().id(userId).email(email).password("password").build();
  // var userDataDao =
  // UserDataDao.builder().userId(userId).firstName(firstName).lastName(lastName).birthDate(birthdate).createdDate(LocalDateTime.now()).lastModifiedDate(LocalDateTime.now()).build();

  // when(userRepository.findAllUsers(organizationId)).thenReturn(Flux.just(userDao));
  // when(userDataRepository.findAllUserData(organizationId)).thenReturn(Flux.just(userDataDao));
  // verify(userRepository).findAllUsers(organizationId);
  // verify(userDataRepository).findAllUserData(organizationId);
}
