{
  "configurations": [
    // FIXME this configuration does not work
    {
      "type": "java",
      "name": "Spring Boot-ServerApplication<stalker-server>",
      "request": "attach",
      "hostName": "localhost",
      "port": 11111,
      "preLaunchTask": {
        "task": "${defaultBuildTask}",
      }
    }
  ]
}
