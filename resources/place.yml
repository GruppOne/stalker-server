type: object
description: Name of the place and reference to geographical informations and places data
properties:
  name:
    type: string
  # reference to Polyline
  polyline:
    $ref: "polyline.yml"
  # reference to place's data
  placeData:
    $ref: "placeData.yml"
required:
  - name
  - position
  - data
