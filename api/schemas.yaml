User:
  $ref: "user.yaml"

Organization:
  $ref: "organization.yaml"

AppConnections:
  $ref: "appConnections.yaml"

CloseSession:
  $ref: "closeSession.yaml"

OrganizationUserCounter:
  $ref: "organizationUserCounter.yaml"

PlaceUserCounter:
  $ref: "placeUserCounter.yaml"

#TODO
UserAccessToPlace:
  type: object
  properties:
    property:
      type: integer
#TODO
UserExitFromPlace:
  type: object
  properties:
    property:
      type: integer
