title: Connection
type: object
description: Represents the fact that a user has connected to an organization.
properties:
  organizationId:
    $ref: ./GenericId.v1.yaml
  userId:
    $ref: ./GenericId.v1.yaml
  creationDateTime:
    $ref: ./GenericTimestamp.v1.yaml
required:
  - organizationId
  - userId
