type: object
description: Represents the fact that an user has connected to an organization.
required:
  - organization
  - user
properties:
  organizationId:
    $ref: "./GenericId.yaml#"
  userId:
    $ref: "./GenericId.yaml#"
  createdDate:
    type: string
    format: date-time
