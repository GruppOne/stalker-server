type: object
description: return the total number of users currently inside a specific organization
properties:
  organizationId:
    $ref: "./genericId.yaml"
  counter:
    type: integer
    format: uint32
    description: indicates the number of users currently inside the organization
required:
  - organizationId
  - counter
